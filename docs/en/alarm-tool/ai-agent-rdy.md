# AI Agent ready feature

- We need to make it ai agent ready
- I think this requires no input but arg instead for log content
- alarm-tool --time 25m --description "Implement authentication flow"
- alarm-tool -t 25m -d "Implement authentication flow"