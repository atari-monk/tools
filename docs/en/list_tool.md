Write me a py cli that prints my tools and description.  
Use json to store data.  
Use structs and functions.  
These are my data
```text
alarm - pomodoro or any time based alarm
extract - extracts message and lines nr form pylance problems info
frame - sprite related
inject - injects file to a template
prompt - generates prompt
rename - rename files
resize - resize image
sprite - merges images to a sprite
```
- Use only names by default
- Use flag -d --desc to print names and description